<!DOCTYPE html>
<html lang="ar">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Join a server GMod [AM4] ZSurvival+Bots (USA)</title>
    <style>
        /* إعدادات سهلة التعديل */
        :root {
            --background-image: url('https://wallpapercave.com/wp/wp2561423.jpg'); /* رابط الخلفية */
            --button-color: #ff6600; /* لون الأزرار */
            --button-hover-color: #ff4500; /* لون الأزرار عند المرور */
            --text-color: white; /* لون النص */
        }

        /* تصميم الخلفية وتأثير الضبابية */
        body {
            background: var(--background-image) no-repeat center center fixed;
            background-size: cover;
            font-family: Arial, sans-serif;
            text-align: center;
            color: var(--text-color);
            margin: 0;
            padding: 0;
            position: relative;
            overflow: hidden;
        }

        body::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.6);
            backdrop-filter: blur(10px);
            z-index: -1;
        }

        /* تحسينات على العنوان */
        h1 {
            margin-top: 50px;
            font-size: 32px;
            text-shadow: 0 0 15px rgba(255, 165, 0, 0.8);
            opacity: 0;
            animation: fadeIn 1.5s forwards;
        }

        /* تأثير ظهور النص */
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(-20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        /* تأثير الكتابة التدريجي */
        #loading-text {
            font-size: 20px;
            white-space: nowrap;
            overflow: hidden;
            border-right: 2px solid white;
            width: 0;
            opacity: 0;
            animation: typing 2s steps(30, end) forwards, blink 0.7s infinite, fadeIn 1s forwards;
        }

        @keyframes typing {
            from { width: 0; }
            to { width: 100%; }
        }

        @keyframes blink {
            50% { border-color: transparent; }
        }

        /* تحسينات الأزرار */
        .button {
            display: block;
            width: 270px;
            margin: 20px auto;
            padding: 15px;
            font-size: 18px;
            color: white;
            background-color: var(--button-color);
            border: 2px solid white;
            border-radius: 10px;
            cursor: pointer;
            transition: all 0.3s ease-in-out;
            text-decoration: none;
            text-align: center;
            box-shadow: 0px 4px 6px rgba(255, 165, 0, 0.5);
            position: relative;
            overflow: hidden;
            opacity: 0;
            transform: translateY(20px);
            animation: fadeInButtons 1s forwards 2s;
        }

        /* تأثير ظهور الأزرار */
        @keyframes fadeInButtons {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        /* تأثير توهج للأزرار عند المرور */
        .button:hover {
            background-color: var(--button-hover-color);
            transform: scale(1.05);
            box-shadow: 0px 0px 15px rgba(255, 69, 0, 0.8);
        }

        /* تأثير نبض خفيف للأزرار */
        @keyframes pulse {
            0% { box-shadow: 0 0 10px rgba(255, 69, 0, 0.5); }
            100% { box-shadow: 0 0 20px rgba(255, 69, 0, 1); }
        }

        /* تأثير عند الضغط */
        .button:active {
            transform: scale(0.95);
        }
    </style>
</head>
<body>
    <h1>Join a server GMod [AM4] ZSurvival+Bots (USA)</h1>
    <p id="loading-text">يتم الآن توجيهك تلقائيًا إلى الخادم...</p>

    <!-- أزرار التفاعل -->
    <a href="steam://connect/209.222.97.134:27017" class="button" onclick="playClickSound()">🔄 إعادة المحاولة للانضمام للخادم</a>
    <a href="https://discord.gg/your-discord-link" class="button" onclick="playClickSound()">💬 انضم إلى Discord</a>
    <a href="https://youtube.com/your-channel-link" class="button" onclick="playClickSound()">📺 اشترك في قناتي</a>

    <!-- كود الصوت -->
    <audio id="clickSound">
        <source src="https://www.fesliyanstudios.com/play-mp3/4381" type="audio/mpeg">
    </audio>

    <script>
        // تشغيل صوت عند الضغط على الأزرار
        function playClickSound() {
            let audio = document.getElementById("clickSound");
            audio.currentTime = 0;
            audio.play();
        }

        // إعادة توجيه تلقائي إلى خادم GMod بعد 3 ثوانٍ
        setTimeout(function() {
            window.location.href = "steam://connect/209.222.97.134:27017";
        }, 3000);

        // إظهار النص بسلاسة
        setTimeout(function() {
            document.getElementById("loading-text").style.opacity = "1";
        }, 500);

        // إخفاء النص بعد 3 ثوانٍ بسلاسة
        setTimeout(function() {
            document.getElementById("loading-text").style.transition = "opacity 1s ease-in-out";
            document.getElementById("loading-text").style.opacity = "0";
        }, 3000);
    </script>
</body>
</html>
