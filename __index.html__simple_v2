<!DOCTYPE html>
<html lang="ar">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Join a server GMod [AM4] ZSurvival+Bots (USA)</title>
    <style>
        /* إعدادات سهلة التعديل */
        :root {
            --background-image: url('https://png.pngtree.com/thumb_back/fh260/background/20230416/pngtree-website-business-blu-ray-image_2446507.jpg'); /* رابط الخلفية */
            --button-color: #ff6600; /* لون الأزرار */
            --button-hover-color: #ff4500; /* لون الأزرار عند المرور */
            --text-color: white; /* لون النص */
        }

        /* تصميم الخلفية وتأثير الإضاءة المتحركة */
        body {
            background: var(--background-image) no-repeat center center fixed;
            background-size: cover;
            font-family: Arial, sans-serif;
            text-align: center;
            color: var(--text-color);
            margin: 0;
            padding: 0;
            position: relative;
            overflow: hidden;
            animation: backgroundShake 5s infinite alternate ease-in-out;
        }

        /* تأثير اهتزاز بسيط للخلفية */
        @keyframes backgroundShake {
            0% { transform: translateX(0); }
            50% { transform: translateX(2px); }
            100% { transform: translateX(-2px); }
        }

        /* تحسينات على العنوان */
        h1 {
            margin-top: 50px;
            font-size: 32px;
            text-shadow: 0 0 20px rgba(255, 165, 0, 0.8);
            opacity: 0;
            animation: fadeIn 1.5s forwards, textGlitch 2s infinite alternate;
        }

        /* تأثير تشويش خفيف على النص */
        @keyframes textGlitch {
            0% { text-shadow: 2px 2px 5px rgba(255, 69, 0, 0.5); }
            100% { text-shadow: -2px -2px 5px rgba(255, 69, 0, 0.5); }
        }

        /* تأثير ظهور النص */
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(-20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        /* تحسينات الأزرار ثلاثية الأبعاد */
        .button {
            display: inline-block;
            width: 270px;
            margin: 20px;
            padding: 15px;
            font-size: 18px;
            color: white;
            background: linear-gradient(145deg, #ff6600, #cc5200);
            border: none;
            border-radius: 10px;
            cursor: pointer;
            transition: all 0.3s ease-in-out;
            text-decoration: none;
            text-align: center;
            box-shadow: 4px 4px 8px rgba(0, 0, 0, 0.6), inset -2px -2px 5px rgba(255, 255, 255, 0.3);
            position: relative;
            transform: perspective(500px) rotateX(10deg);
        }

        /* تأثير ثلاثي الأبعاد عند المرور */
        .button:hover {
            background: linear-gradient(145deg, #ff4500, #cc3300);
            transform: perspective(500px) rotateX(5deg) scale(1.05);
            box-shadow: 6px 6px 12px rgba(0, 0, 0, 0.8), inset -3px -3px 6px rgba(255, 255, 255, 0.2);
        }

        /* تأثير اهتزاز للأزرار عند الضغط */
        .button:active {
            animation: shake 0.2s ease-in-out;
            transform: perspective(500px) rotateX(0deg) scale(0.98);
        }

        @keyframes shake {
            0% { transform: translateX(0); }
            25% { transform: translateX(-3px); }
            50% { transform: translateX(3px); }
            75% { transform: translateX(-3px); }
            100% { transform: translateX(0); }
        }
    </style>
</head>
<body>
    <h1>Join a server GMod [AM4] ZSurvival+Bots (USA)</h1>
    <p id="loading-text">يتم الآن توجيهك تلقائيًا إلى الخادم...</p>

    <!-- أزرار التفاعل ثلاثية الأبعاد -->
    <a href="steam://connect/209.222.97.134:27017" class="button" onclick="playClickSound()">🔄 إعادة المحاولة للانضمام للخادم</a>
    <a href="https://discord.gg/your-discord-link" class="button" onclick="playClickSound()">💬 انضم إلى Discord</a>
    <a href="https://youtube.com/your-channel-link" class="button" onclick="playClickSound()">📺 اشترك في قناتي</a>

    <!-- كود الصوت -->
    <audio id="clickSound">
        <source src="https://www.fesliyanstudios.com/play-mp3/4381" type="audio/mpeg">
    </audio>

    <script>
        // تشغيل صوت عند الضغط على الأزرار
        function playClickSound() {
            let audio = document.getElementById("clickSound");
            audio.currentTime = 0;
            audio.play();
        }

        // إعادة توجيه تلقائي إلى خادم GMod بعد 3 ثوانٍ
        setTimeout(function() {
            window.location.href = "steam://connect/209.222.97.134:27017";
        }, 3000);

        // إظهار النص بسلاسة
        setTimeout(function() {
            document.getElementById("loading-text").style.opacity = "1";
        }, 500);

        // إخفاء النص بعد 3 ثوانٍ بسلاسة
        setTimeout(function() {
            document.getElementById("loading-text").style.transition = "opacity 1s ease-in-out";
            document.getElementById("loading-text").style.opacity = "0";
        }, 3000);
    </script>
</body>
</html>
