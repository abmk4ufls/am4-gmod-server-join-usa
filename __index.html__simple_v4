<!DOCTYPE html>
<html lang="ar">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Join a server GMod [AM4] ZSurvival+Bots (USA)</title>
    <style>
        /* 🎨 تخصيص سريع للألوان والتأثيرات */
        :root {
            --background-gradient: linear-gradient(45deg, #ff0000, #ff7300, #ffcc00, #00ffcc, #0044ff, #cc00ff);
            --button-bg: rgba(0, 0, 0, 0.8);
            --button-hover: #ffcc00;
            --text-color: white;
            --glow-color: rgba(255, 204, 0, 0.9);
            --title-bg: rgba(0, 0, 0, 0.7); /* خلفية شفافة للعنوان */
        }

        /* 🌌 الخلفية المتحركة */
        body {
            font-family: 'Arial', sans-serif;
            text-align: center;
            color: var(--text-color);
            margin: 0;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            flex-direction: column;
            position: relative;
            overflow: hidden;
            background: black;
        }

        /* 🌈 تأثير الخلفية المتحركة */
        .animated-bg {
            position: absolute;
            width: 100%;
            height: 100%;
            background: var(--background-gradient);
            background-size: 400% 400%;
            animation: backgroundMove 8s infinite alternate ease-in-out;
            filter: blur(50px);
        }

        @keyframes backgroundMove {
            0% { background-position: 0% 50%; }
            100% { background-position: 100% 50%; }
        }

        /* 🔥 تحسين العنوان */
        .title-container {
            background: var(--title-bg);
            padding: 15px 30px;
            border-radius: 10px;
            box-shadow: 0px 0px 15px var(--glow-color);
            animation: fadeIn 2s ease-in-out, pulseGlow 2s infinite alternate;
        }

        h1 {
            font-size: 2.8rem;
            text-shadow: 2px 2px 10px black, 0 0 20px var(--glow-color);
            -webkit-text-stroke: 1px black; /* إضافة حد أسود حول النص */
            color: white;
            animation: waveText 3s infinite alternate ease-in-out;
        }

        @keyframes waveText {
            0% { transform: scale(1); }
            100% { transform: scale(1.05); }
        }

        @keyframes pulseGlow {
            0% { box-shadow: 0px 0px 10px var(--glow-color); }
            100% { box-shadow: 0px 0px 25px var(--glow-color); }
        }

        /* 🔘 تصميم الأزرار */
        .btn-container {
            margin-top: 20px;
        }

        .btn {
            display: block;
            width: 300px;
            padding: 15px;
            margin: 10px auto;
            font-size: 18px;
            font-weight: bold;
            color: var(--text-color);
            background: var(--button-bg);
            border: 2px solid var(--button-hover);
            border-radius: 10px;
            cursor: pointer;
            text-decoration: none;
            position: relative;
            overflow: hidden;
            text-transform: uppercase;
            transition: all 0.3s ease-in-out;
            box-shadow: 0px 0px 15px var(--glow-color);
            animation: pulseEffect 2s infinite alternate ease-in-out;
        }

        /* ✨ تأثير عند تمرير الماوس */
        .btn:hover {
            background: var(--button-hover);
            color: black;
            box-shadow: 0px 0px 30px var(--glow-color);
            transform: scale(1.1);
            filter: blur(1px);
        }

        /* ✨ تأثير الضوء المتحرك للأزرار */
        .btn::before {
            content: "";
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: rgba(255, 255, 255, 0.3);
            transform: skewX(-30deg);
            transition: all 0.5s ease-in-out;
        }

        .btn:hover::before {
            left: 100%;
        }

        @keyframes pulseEffect {
            0% { transform: scale(1); }
            100% { transform: scale(1.05); }
        }

    </style>
    <script>
        // 🔊 تشغيل الصوت عند تمرير الماوس فوق الأزرار
        function playHoverSound() {
            let audio = new Audio('https://www.myinstants.com/media/sounds/click.mp3'); // يمكنك استبدال الرابط بصوت مفضل لديك
            audio.volume = 0.2;
            audio.play();
        }

        document.addEventListener("DOMContentLoaded", function () {
            document.querySelectorAll(".btn").forEach(button => {
                button.addEventListener("mouseenter", playHoverSound);
            });
        });

        // 🚀 التوجيه التلقائي بعد 3 ثوانٍ إلى سيرفر GMod
        window.onload = function() {
            setTimeout(() => {
                window.location.href = "steam://connect/209.222.97.134:27017";
            }, 3000);
        }
    </script>
</head>
<body>

    <div class="animated-bg"></div> <!-- 🔥 خلفية متحركة نيون -->

    <div class="title-container">
        <h1>Join a server GMod [AM4] ZSurvival+Bots (USA)</h1>
    </div>

    <div class="btn-container">
        <a class="btn" href="steam://connect/209.222.97.134:27017">🔄 حاول الانضمام مجددًا</a>
        <a class="btn" href="https://discord.gg/YOUR_DISCORD_LINK" target="_blank">💬 انضم إلى Discord</a>
        <a class="btn" href="https://www.youtube.com/YOUR_CHANNEL" target="_blank">▶ اشترك في القناة</a>
    </div>

</body>
</html>
